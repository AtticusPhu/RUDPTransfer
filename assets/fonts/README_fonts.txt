Optional bundled UI fonts go here.

To use a bundled Chinese font, place a legally usable .ttf, .ttc, or .otf file in this folder before running PyInstaller.
The application will load assets/fonts first, then fall back to system fonts such as Microsoft YaHei.

Font files are not included in this patch.
